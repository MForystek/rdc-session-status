# Remote Desktop Connection status

Simple application that exposes the endpoint on the port `2137` that shows if any user is currently connected to the machine using Windows Remote Desktop Connection.

Application run in the background being present only as a System Tray icon.

## Run

Run using:

```
.\launch.ps1
```